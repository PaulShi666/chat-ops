# Spring settings
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chat_ops?characterEncoding=UTF-8
    username: root
    password: 111111
    driver-class-name: com.mysql.jdbc.Driver
# Server settings
server:
  port: 8080
  compression:
    enabled: true
# mybatis settings
#mybatis:
#  type-aliases-package: com.sse.chatops.dao
#  mapper-locations: classpath:dao/*.xml
